<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ClotingStoreManagementApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/toolbar_color</item>
    </style>

    <style name="Theme.ClotingStoreManagementApp" parent="Base.Theme.ClotingStoreManagementApp" />
</resources>